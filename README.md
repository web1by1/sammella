
  # Rounded Grid Interface

  This is a code bundle for Rounded Grid Interface adapted from figma.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  